/* =========================================================================
 * usart.h —— 屏蔽层
 *
 * 这个文件故意和 Core/Inc/usart.h 同名。
 * 编译时 -I. 排在 -I..\Core\Inc 前面，所以 modbus_slave.c 里
 *     #include "usart.h"
 * 会命中本文件（转发到 HAL 替身），而不是真实的那个会拖进整个 HAL 的头文件。
 *
 * 别删这个文件，删了仿真台就编译不过。
 * ========================================================================= */
#ifndef USART_STUB_H
#define USART_STUB_H

#include "hal_stub.h"

#endif
