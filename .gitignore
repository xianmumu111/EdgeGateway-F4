*.exe
*.o
*.obj
*.elf
*.map
*.lst
*.bin
*.hex
*.axf
*.lnp
*.sct
*.bak

# Keil 工程 GUI 布局文件：含本机绝对路径，每台机器都不一样，必须忽略
*.uvgui.*
*.uvguix.*
# .uvoptx = 调试配置（断点/Flash算法），同样含本机路径，且每次调试后都会变。
# 忽略后 Keil 打开工程会自动重建，代价只是要重选一次 Flash 下载算法。
*.uvoptx
*.build_log.htm
*.crf
*.dep
*.d
*.iex
*.htm
*.sfr

Output/
Listings/
Objects/
Debug/
Release/

# Keil 编译输出目录名 = 工程名，不能用 Objects/ 覆盖，单独忽略
MDK-ARM/Objects/
MDK-ARM/Listings/
/nodes/f103_slave/MDK-ARM/F103_ModbusSlave/

# CubeMX 生成的 HAL + CMSIS：68MB，其中 48MB 是 DSP 静态库和例程，永远用不到。
# 用 CubeMX 打开 .ioc 点 Generate Code 即可完整重建，不需要进版本库。
# ⚠️ 每新建一个节点工程，都要在这里补一条，否则 57MB 直接进版本库。
/nodes/f103_slave/Drivers/
/nodes/f407_gateway/Drivers/

# CubeMX 生成的 Middleware（勾选 FREERTOS 后出现 Middlewares/Third_Party/FreeRTOS）。
# 源码由 CubeMX Repository 复制而来，Generate 即可重建，不需要进版本库。
# ⚠️ 注意：保留 .ioc 里勾选状态即可，clone 后 Generate Code 会重新生成。
/nodes/f407_gateway/Middlewares/

# Keil 编译输出目录名 = 工程名（f407_gateway），通用规则覆盖不到
/nodes/f407_gateway/MDK-ARM/f407_gateway/

.vs/
.vscode/
